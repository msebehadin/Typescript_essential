# Typescript Essentials

A simple project to demonstrate Typescript basics.

## 📁 Project Structure

This repository contains a folder named `ts-basics` with five Typescript files, each covering a fundamental concept:

- `variables.ts` – Demonstrates variable declarations and updates.
- `functions.ts` – Contains a function that returns a personalized greeting.
- `objects.ts` – Defines and prints properties of a car object.
- `arrays.ts` – Creates an array of fruits and prints each item.
- `async.ts` – Shows asynchronous behavior using `setTimeout`.

## 👨‍💻 Author

Mehammed Sebehadin
