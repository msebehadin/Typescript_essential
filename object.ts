type Car={
    brand:string;
    model:String;
    year:number
}

const car:Car={
brand:'suzuki',
model:'desire',
year:2021
}
console.log(`brand:${car.brand}`)
console.log(`model:${car.model}`)
console.log(`year:${car.year}`)